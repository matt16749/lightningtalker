<div class="ind-student">
  <h1><%= "#{@student.first_name} #{@student.last_name}" %><br>
    <small>Address: <%= @student.address %></small><br>
    <small>City: <%= @student.city %></small><br>
    <small>State: <%= @student.state %></small><br>
    <small>Zip: <%= @student.zip %></small>
  </h1>
</div>